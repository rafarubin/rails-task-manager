<h1>My Tasks</h1>
<div>
  <ul>
    <% @tasks.each do |task| %>
    <li>
      <%= link_to task_path(task.id) do %>
        <strong>
          <%= task.title %>
        </strong>
      <% end %>
    </li>
  <% end %>
  </ul>
</div>

<%= link_to '+ Add new task', new_task_path %>
